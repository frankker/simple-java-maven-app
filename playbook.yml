- hosts: localhost
  become: yes
  become_method: sudo
  vars:
    jenkins_hostname: 172.21.53.1
    docker_users:
      - jenkins
  roles:
    - role: geerlingguy.jenkins
    - role: geerlingguy.docker