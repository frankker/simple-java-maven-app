- hosts: localhost
  become_method: enable
  vars:
    ansible_become_pass: chingiap83
    jenkins_hostname: localhost
    docker_users:
      - jenkins
  roles:
    - role: geerlingguy.jenkins
    - role: geerlingguy.docker